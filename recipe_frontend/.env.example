# Base URL for the recipe backend API (recipe_database container)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Site URL for auth redirects (used by hosted auth providers if any)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
